<% if Current.customer %>
  <%= turbo_frame_tag "#{dom_id(comment)}_actions" do %>
      <%= render 'comments/comment/actions', comment: comment %>
  <% end %>
<% else %>
  <%= turbo_frame_tag "#{dom_id(comment)}_actions", src: comment_actions_path(comment) %>
<% end %>
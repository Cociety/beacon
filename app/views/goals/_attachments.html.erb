<div>
  <span class="form-label"><%= t('attachments') %></span>
  <ul class="max-h-60 overflow-y-scroll">
    <div class="grid grid-cols-1 gap-4 sm:grid-cols-2">
      <%= render goal.attachments %>
    </div>
  </ul>
  <% if policy(goal).edit? %>
    <%= render 'attachments/form', attachable: goal %>
  <% end %>
</div>